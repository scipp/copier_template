# Installation

To install {{prettyname}} and all of its dependencies, use

`````{tab-set}
````{tab-item} pip
```sh
pip install {{projectname}}
```
````
````{tab-item} conda
```sh
conda install -c conda-forge {{projectname}}
```
````
`````
