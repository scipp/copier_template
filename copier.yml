projectname:
  type: str
  help: What is your project name?
orgname:
  default: scipp
  type: str
  help: What is your organization name?
description:
  type: str
  help: Brief description?
min_python:
  type: str
  help: Minimum Python version?
max_python:
  type: str
  help: Maximum Python version that is known to work?
year: 2023
nightly_deps:
  type: str
  help: List of dependencies to install from Git during nightly builds, separated by commas. Make sure to also list transitive dependencies, otherwise their released versions will be used.